# hk-llt

Part of the Huvudkontoret workspace. Start with `hk context hk-llt`, then read
[AGENTS.md](AGENTS.md) for repository instructions,
[CONTEXT.md](CONTEXT.md) for domain language, and [README.md](README.md) for setup.

Use `hk verify hk-llt` for the local verification gate. The local workspace
uses the umbrella layout. For a feature worktree, use
`hk verify hk-llt --worktree <name>` and run pnpm scripts from that worktree.
