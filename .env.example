# PROTOTYPE ONLY. Copy to .env and keep the real values local.
DATA_MODE=live
TRAFIKLAB_API_KEY=
RESROBOT_API_KEY=

# Optional non-secret public Worker URL. HTTPS is required outside localhost.
MCP_PUBLIC_URL=https://worker.example.com

# Optional: GTFS Sverige 2 agency_id for Luleå Lokaltrafik. Responses are
# always filtered by operator name as well, so this is only an upstream
# optimisation once the current ID has been verified.
RESROBOT_LLT_OPERATOR_ID=
