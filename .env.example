# Live local development only. Keep real values in an untracked .env file.
TRAFIKLAB_API_KEY=
RESROBOT_API_KEY=

# Local host and mode are selected by the local/mock Wrangler environments.
# Production MCP_PUBLIC_URL is configured in wrangler.jsonc.
